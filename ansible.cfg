[defaults]
inventory = inventories/inventory.yml
host_key_checking = false
remote_user = vagrant

[ssh_connection]
pipelining = true

[privilege_escalation]
become = true
become_method = sudo
become_user = root
become_ask_pass = false
