---
profile: production
exclude_paths:
  - .ansible/
  # Encrypted; CI has no vault secret — avoids noisy decrypt warnings during lint.
  - inventories/**/vault.yml

skip_list:
  - var-naming[no-role-prefix]
  - name[play]
  - no-handler
  - args
